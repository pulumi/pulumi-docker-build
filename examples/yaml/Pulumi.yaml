name: provider-docker-native
runtime: yaml
plugins:
  providers:
    - name: docker-native
      path: ../../bin

resources:
  myRandomResource:
    type: docker-native:Random
    properties:
      length: 24

outputs:
  output:
    value: ${myRandomResource.result}
