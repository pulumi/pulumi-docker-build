name: provider-dockerbuild
runtime: yaml
plugins:
  providers:
    - name: dockerbuild
      path: ../../bin

resources:
  myRandomResource:
    type: dockerbuild:Random
    properties:
      length: 24

outputs:
  output:
    value: ${myRandomResource.result}
